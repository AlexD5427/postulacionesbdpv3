'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { useMemo, useState } from 'react'; import { AuthShell } from '@/components/features/AuthShell'; import { useAuth } from '@/components/providers/AuthProvider'; import { useI18n } from '@/components/providers/I18nProvider'; import { useToast } from '@/components/providers/ToastProvider'; import { GlassButton } from '@/components/ui/glass'; import { DEPARTAMENTOS } from '@/lib/data/convocatorias';
function fuerza(password:string){let p=0;if(password.length>=8)p++;if(password.length>=12)p++;if(/[A-Z]/.test(password)&&/[a-z]/.test(password))p++;if(/\d/.test(password))p++;if(/[^A-Za-z0-9]/.test(password))p++;const escala=[{etiqueta:'—',color:'var(--line)'},{etiqueta:'Baja',color:'var(--danger)'},{etiqueta:'Media',color:'var(--warn)'},{etiqueta:'Buena',color:'var(--info)'},{etiqueta:'Alta',color:'var(--ok)'},{etiqueta:'Muy alta',color:'var(--ok)'}];return {nivel:p,...escala[p]};}
export default function RegistroPage(){const {t}=useI18n();const {register}=useAuth();const {toast}=useToast();const router=useRouter();const [form,setForm]=useState({nombre:'',apellido:'',email:'',password:'',telefono:'',ci:'',ciudad:'La Paz',departamento:'La Paz'});const [acepta,setAcepta]=useState(false);const [error,setError]=useState<string|null>(null);const [enviando,setEnviando]=useState(false);const robustez=useMemo(()=>fuerza(form.password),[form.password]);const cambiar=(campo:keyof typeof form)=>(e:React.ChangeEvent<HTMLInputElement|HTMLSelectElement>)=>setForm(p=>({...p,[campo]:e.target.value}); const enviar=async(e:React.FormEvent)=>{e.preventDefault();if(!form.nombre.trim()||!form.apellido.trim()){setError('auth.error.required');return;}if(!acepta){setError('auth.error.terms');return;}setEnviando(true);const r=await register({...form});setEnviando(false);if(!r.ok){setError(r.error??'error.generic');return;}setError(null);toast(t('auth.registered'));router.push('/panel?tab=perfil');};return <AuthShell titulo={t('auth.register.title')} subtitulo={t('auth.register.subtitle')} pie={<>{t('auth.hasAccount')}{' '}<Link href="/login" className="link-underline" style={{color:'var(--accent)'}}>{t('nav.login')}</Link></>}><form onSubmit={enviar} className="stack gap-md" noValidate><div className="grid cols-2 gap-sm"><div className="field"><label className="field__label" htmlFor="nombre">{t('auth.name')}</label><input id="nombre" className="input" required value={form.nombre} onChange={cambiar('nombre')} autoComplete="given-name"/></div><div className="field"><label className="field__label" htmlFor="apellido">{t('auth.lastname')}</label><input id="apellido" className="input" required value={form.apellido} onChange={cambiar('apellido')} autoComplete="family-name"/></div></div><div className="field"><label className="field__label" htmlFor="email">{t('auth.email')}</label><input id="email" className="input" type="email" required value={form.email} onChange={cambiar('email')} autoComplete="email" aria-invalid={!!error}/></div><div className="field"><label className="field__label" htmlFor="password">{t('auth.password')}</label><input id="password" className="input" type="password" required minLength={8} value={form.password} onChange={cambiar('password')} autoComplete="new-password" aria-invalid={!!error}/><div className="progress" aria-hidden="true" style={{marginTop:'0.4rem'}}><i style={{width:`${(robustez.nivel/5)*100}%`,background:robustez.color}}/></div><span className="field__hint">{t('auth.passwordHint')} · {robustez.etiqueta}</span></div><div className="grid cols-2 gap-sm"><div className="field"><label className="field__label" htmlFor="ci">{t('auth.ci')}</label><input id="ci" className="input" value={form.ci} onChange={cambiar('ci')} inputMode="numeric"/></div><div className="field"><label className="field__label" htmlFor="telefono">{t('auth.phone')}</label><input id="telefono" className="input" value={form.telefono} onChange={cambiar('telefono')} type="tel" autoComplete="tel"/></div></div><div className="grid cols-2 gap-sm"><div className="field"><label className="field__label" htmlFor="ciudad">{t('auth.city')}</label><input id="ciudad" className="input" value={form.ciudad} onChange={cambiar('ciudad')} autoComplete="address-level2"/></div><div className="field"><label className="field__label" htmlFor="departamento">{t('profile.basics')}</label><select id="departamento" className="select" value={form.departamento} onChange={cambiar('departamento')}>{DEPARTAMENTOS.map(d=><option key={d} value={d}>{d}</option>)}</select></div></div><label className="check"><input type="checkbox" checked={acepta} onChange={e=>setAcepta(e.target.checked)}/><span>{t('auth.terms')}</span></label>{error&&<p className="field__error" role="alert">{t(error)}</p>}<GlassButton type="submit" variant="primary" block arrow disabled={enviando}>{enviando?t('common.loading'):t('auth.submitRegister')}</GlassButton><p className="faint" style={{fontSize:'var(--fs-xs)'}}>{t('auth.demoNote')}</p></form></AuthShell>;}
